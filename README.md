# Overview

This repository is for studying RTSP protocol step-by-step, implementing it from scratch.

## Prerequisites

- WSL2 Ubuntu 24.04

You can install dependencies by running below command
```bash
sudo apt update
sudo apt install ffmpeg
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```
